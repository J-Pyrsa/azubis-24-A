/**
 * 
 * @param a minuendo
 * @param b sustraendo
 * @returns Diferencia
 */
function resta(a: number, b: number): number {
    return a - b;
}

const resultado: number = resta(10, 5);
console.log("El resultado de la resta es:", resultado);
